---
machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - ./circleci-builder.sh:
        parallel: true

test:
  override:
    - ./circleci-tester.sh:
        parallel: true
